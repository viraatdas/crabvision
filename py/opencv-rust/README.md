# opencv-rust (meta)

This is a tiny meta-package that depends on `crabvision`, a Rust-native `cv2` module.

Install either of the following:

```
pip install crabvision
```

or

```
pip install opencv-rust
```

Both provide `import cv2`.
